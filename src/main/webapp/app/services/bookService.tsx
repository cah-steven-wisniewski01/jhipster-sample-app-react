import axios from 'axios';

export async function getBook(bookId: number) {
  // TODO: Fill in body

  // Added res to avoid build errors
  const res = { data: [] };
  return res.data;
}

export async function updateCurrentHolder(bookId: number, userName: string) {
  // TODO: Fill in body

  // Added res to avoid build errors
  const res = { data: [] };
  return res.data;
}

export async function addToReservationList(bookId: number, userName: string) {
  // TODO: Fill in body

  // Added res to avoid build errors
  const res = { data: [] };
  return res.data;
}
